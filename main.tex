%% ============================================================
%% main.tex -- metadata, abstract, contributions, and section arc
%% Build: latexmk -pdf main.tex
%% ============================================================
\documentclass[10pt]{article}

\input{preamble/style}
\input{preamble/macros}

\reporttitle{Merkle Commitments for \textsf{Arkworks}}
\reportauthors{%
  \begin{tabular}{c@{\hspace{3em}}c}
    Alexander J. Havlin & Andrew Zitek-Estrada \\
    \texttt{alexander.havlin@epfl.ch} & \texttt{andrew.zitek@epfl.ch} \\
    EPFL & EPFL
  \end{tabular}
}
\reportdate{June 2026}

\begin{document}

\makereporttitle

\begin{abstract}
Merkle commitments are the hash-based vector commitments that post-quantum
proving systems are compiled through, so an argument inherits its soundness and
zero knowledge directly from the commitment layer beneath it.  A correct
configuration is subtle: binding and hiding each force a parameter, a digest
width and a per-leaf salt cardinality, that grows with field size and cannot be
reused unchanged across fields.  The defaults shipped with \textsf{Arkworks}'
\textsf{ark-mt} miss this for small fields, leaving both the digest and the salt
near $30$ bits against a $\secpar=128$ target and breaking binding and hiding at
once.  Sizing these parameters should be the infrastructure's obligation, not the
user's: \textsf{ark-vc} fixes the interface between the security definitions and
interchangeable hash implementations, and a single rule maps a configuration
$(\field,\secpar,\RO,\text{interface})$ to the digest width, salt cardinality,
and query budget it must meet, so an undersized choice is \emph{detected and
refused} rather than shipped.  We provide a few secure example hashers, but the
design exists so that a developer can supply their own hash and receive the
parameters that make it secure.  The report treats binding and hiding in turn,
unifies their sizing under that rule, and measures its cost.  A companion Lean~4
formalisation, one way to discharge these obligations, gives the binding axis
machine-checked standing and certifies the salt-cardinality lemmas hiding needs,
leaving the random-oracle composition as the single named open obligation.
\end{abstract}

% \reportkeywords{vector commitments; Merkle trees; random oracle model; Lean~4;
%   binding; hiding; \textsf{ark-vc}}

% \paragraph{Contributions.}
% This report makes four contributions.  First, a security-requirements spine for
% the commitment layer: a single rule mapping a configuration $(\field,\secpar,H)$
% to the digest width, salt cardinality, and query budget it must meet, derived
% once and reused across fields.  Second, the binding axis, sized by that rule and
% machine-checked end to end over a general Merkle shape, with the binary tree as
% the exhibited instance.  Third, an honest hiding contract: a
% typed-randomness interface and machine-checked salt-cardinality lemmas, with the
% random-oracle composition named as the one obligation still open.  Fourth, the
% developer payoff: a single place to configure a commitment safely, where
% under-specification is refused rather than silently accepted.  \Cref{tab:claims}
% records the status of each claim.

\input{sections/motivation}
\input{sections/binding}
\input{sections/hiding}
\input{sections/spine}
\input{sections/cost}
\input{sections/conclusion}

\begingroup
\footnotesize
\makeatletter
\renewcommand\section{\@startsection{section}{1}{\z@}%
  {-0.8ex \@plus -0.3ex}{0.4ex}{\normalfont\large\bfseries}}
\makeatother
\bibliographystyle{alpha}
\bibliography{references}
\endgroup

\end{document}
