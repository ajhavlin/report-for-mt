\begin{table}[tb]
  \centering
  \footnotesize
  \caption{Evidence register: the strongest claim each axis of the current
    Lean~4 artefact currently supports.}
  \label{tab:claims}
  \begin{tabularx}{\linewidth}{@{}>{\bfseries}lXl@{}}
    \toprule
    Claim & Defensible statement & Status \\
    \midrule
    Merkle ROM binding
      & A localised disputed overlap has advantage $\le\collisionBound(\kappa,q)$
        (\code{binding\_win\_le\_trace\_collision},
        \code{coupling\_trace\_le\_collisionBound}).
      & machine-checked \\
    Birthday probability
      & \code{birthdayBound}: $n$ uniform samples collide w.p.\ $\le n(n-1)/(2|R|)$;
        union bound, no sorry.
      & machine-checked \\
    Concrete sizing
      & \code{ofField} meets the decidable \code{MeetsBindingTarget}; BabyBear
        $\secpar{=}128$, $h{=}64$ gives $D{=}9$, $\kappa{=}270$ (\texttt{native\_decide}).
      & machine-checked \\
    Salt-cardinality lemmas
      & $k{=}5$ field salts and $S{=}16$ byte salts each define a space
        $\ge2^{128}$ over BabyBear (\code{babyBear\_*\_salt\_hiding}).
      & machine-checked \\
    Hiding interface
      & Hiding randomness is an explicit typed \code{Randomness} input;
        \code{MerkleHasher.sampleSalt} removed.
      & implemented \\
    Root hiding obligation
      & \code{HasROMHiding} declares the root games and a TV-distance bound; no
        Merkle instance or selective-opening field yet.
      & intended reduction \\
    ROM extraction
      & \code{cacheExtract\_sound}: extractor consistent with accepted openings
        on collision-free traces.
      & $\circ$ (1~sorry) \\
    Equivocation
      & Distinct from trapdoor equivocability; requires a programmable oracle.
      & out of scope \\
    \bottomrule
  \end{tabularx}
\end{table}
